import { PageLoader } from "@/components/ui/loading";

const HomologationLoadingSkeleton = () => (
  <PageLoader variant="dashboard" message="Carregando homologações..." />
);

export default HomologationLoadingSkeleton;
